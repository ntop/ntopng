<div class="notification-container flex-column d-flex align-items-end" {{ (container_id == nil and "" or "id=".. container_id .."") }}>
    {% for key, notification in ipairs(notifications) do %}
        {* notification:render() *}
    {% end %}
</div>