Prerequisites
-------------

- curl
- jq
- redis-cli
- shyaml (pip install shyaml)

How to Run The Tests
--------------------

Perform all the tests running:

$ do.sh

In order to run a specific test, specify the name of the yaml file:

$ ./do.sh get_host_active_01.yaml   
