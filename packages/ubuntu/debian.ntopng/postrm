#!/bin/sh -e

set -e

if [ "$1" = "purge" ] ; then
    if [ -f /bin/systemctl ] ; then
	if systemctl  list-unit-files | grep ntopng.service; then
	    systemctl disable ntopng.service
	fi
    else
	update-rc.d ntopng remove >/dev/null
    fi
fi

exit 0
