---------------------------------
--! @file
--! @brief The 'ntop' table.
---------------------------------

--! @brief Set the active interface by id.
--! @param id the interface id.
--! @return return the interface name on success, nil otherwise.
function setActiveInterfaceId(id)

--! @brief Get the ntopng interface names.
--! @return return a table with the interface names.
function getIfNames()

--! @brief Find a network interface and set it as global variable.
--! @param ifname the interface name.
function select(ifname)

